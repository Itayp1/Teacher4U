import React from "react";
import { View, Text, StyleSheet } from "react-native";

const SigninScreenScreen = () => {
  return (
    <View>
      <Text>sign in screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default SigninScreenScreen;
